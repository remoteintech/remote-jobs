---
layout: base
permalink: /index.html
title: 'Remote In Tech'
description: 'A list of semi to fully remote-friendly companies in tech'
---

<div class="wrapper flow region">
  <header class="flow">
    <h1 class="gradient-text">Remote In Tech</h1>
    <p class="text-step-1">
      A list of semi to fully remote-friendly companies in or around tech.
    </p>
  </header>

  <div>
    <p>
      See a company missing? Want to update information?
      <a href="/contributing/">Contribute on GitHub</a>!
    </p>
  </div>

  <!-- Search Companies -->
  <div class="search-container">
    <div id="search"></div>
  </div>

  <script>
    window.addEventListener('DOMContentLoaded', (event) => {
      new PagefindUI({
        element: "#search",
        showSubResults: true,
        showImages: false,
        excerptLength: 15,
        placeholder: "Search companies..."
      });
    });
  </script>

  <!-- Company Table -->
  <div class="company-list-container region">
    <table class="company-table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Website</th>
          <th>Region</th>
        </tr>
      </thead>
      <tbody>
        {% for company in collections.companies %}
        <tr>
          <td data-label="Name">
            <a href="{{ company.url }}">{{ company.data.name }}</a>
          </td>
          <td data-label="Website">
            <a href="{{ company.data.website }}"
               target="_blank"
               rel="noopener noreferrer"
               class="no-indicator">
              {{ company.data.website | replace("https://", "") | replace("http://", "") }}
            </a>
          </td>
          <td data-label="Region">{{ company.data.region | truncate(80) }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>

    <p class="company-count">
      Total: {{ collections.companies.length }} companies
    </p>
  </div>
</div>

{%- css "local" -%}
  /* Search container */
  .search-container {
    margin-block: var(--space-m);
  }

  /* Pagefind UI dark mode styles */
  .pagefind-ui__search-input {
    background-color: var(--color-bg) !important;
    border-color: var(--color-bg-accent) !important;
    color: var(--color-text) !important;
  }

  .pagefind-ui__search-input::placeholder {
    color: var(--color-text-accent) !important;
  }

  .pagefind-ui__search-input:focus {
    border-color: var(--color-primary) !important;
  }

  .pagefind-ui__search-clear {
    background-color: var(--color-bg-accent) !important;
    color: var(--color-text) !important;
  }

  .pagefind-ui__search-clear:hover {
    background-color: var(--color-bg-accent-2) !important;
  }

  .pagefind-ui__drawer {
    background-color: var(--color-bg) !important;
    border-color: var(--color-bg-accent) !important;
  }

  .pagefind-ui__result {
    background-color: var(--color-bg) !important;
    border-color: var(--color-bg-accent) !important;
  }

  .pagefind-ui__result:hover {
    background-color: var(--color-bg-accent) !important;
  }

  .pagefind-ui__result-link {
    color: var(--color-text) !important;
    text-decoration: none !important;
  }

  .pagefind-ui__result-link:hover {
    color: var(--color-primary) !important;
  }

  .pagefind-ui__result-title {
    color: var(--color-primary) !important;
  }

  .pagefind-ui__result-excerpt {
    color: var(--color-text) !important;
  }

  .pagefind-ui__result-nested {
    color: var(--color-text-accent) !important;
  }

  .pagefind-ui__message {
    color: var(--color-text) !important;
  }

  .pagefind-ui__button {
    background-color: var(--color-bg-accent) !important;
    border-color: var(--color-bg-accent-2) !important;
    color: var(--color-text) !important;
  }

  .pagefind-ui__button:hover {
    background-color: var(--color-bg-accent-2) !important;
  }

  .pagefind-ui mark,
  mark {
    background-color: var(--color-tertiary) !important;
    color: var(--color-dark) !important;
  }

  /* Override global table styles for company table */
  .company-list-container {
    --region-space: var(--space-m);
  }

  .company-table {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--size-step--1);
    display: table;
    margin-block-start: var(--space-s);
    table-layout: fixed;
  }

  .company-table thead {
    display: table-header-group;
    position: static;
    clip: auto;
    height: auto;
    width: auto;
    margin: 0;
    overflow: visible;
  }

  .company-table tr {
    display: table-row;
    border: none;
    margin: 0;
  }

  .company-table th,
  .company-table td {
    display: table-cell;
    text-align: left;
    padding: var(--space-3xs) var(--space-xs);
    border-bottom: 1px solid var(--color-bg-accent);
    vertical-align: top;
    line-height: 1.4;
    word-wrap: break-word;
    overflow-wrap: break-word;
  }

  .company-table td::before {
    display: none;
  }

  .company-table th {
    font-weight: var(--font-bold);
    background: var(--color-bg-accent);
    position: sticky;
    top: 0;
    z-index: 10;
    padding: var(--space-xs) var(--space-xs);
  }

  .company-table tbody tr:hover {
    background: var(--color-bg-accent);
  }

  .company-table td:first-child {
    font-weight: var(--font-bold);
  }

  /* Column widths */
  .company-table th:nth-child(1),
  .company-table td:nth-child(1) {
    width: 25%;
  }

  .company-table th:nth-child(2),
  .company-table td:nth-child(2) {
    width: 30%;
  }

  .company-table th:nth-child(3),
  .company-table td:nth-child(3) {
    width: 45%;
    max-width: 0; /* Force wrapping */
  }

  /* Truncate long website URLs */
  .company-table td:nth-child(2) {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .company-count {
    margin-block-start: var(--space-xs);
    font-style: italic;
    color: var(--color-text-accent);
    text-align: center;
  }

  @media (max-width: 768px) {
    .company-table {
      font-size: var(--size-step--2);
    }

    .company-table th,
    .company-table td {
      padding: var(--space-3xs) var(--space-2xs);
    }

    /* Stack on mobile */
    .company-table thead {
      clip: rect(0 0 0 0);
      height: 1px;
      margin: -1px;
      overflow: hidden;
      padding: 0;
      position: absolute;
      width: 1px;
    }

    .company-table tr {
      display: block;
      margin-block-end: var(--space-s);
      border: 1px solid var(--color-bg-accent);
    }

    .company-table td {
      display: block;
      border-bottom: 1px solid var(--color-bg-accent);
      padding: var(--space-xs);
    }

    .company-table td:last-child {
      border-bottom: none;
    }

    .company-table td::before {
      content: attr(data-label);
      display: inline-block;
      font-weight: var(--font-bold);
      margin-inline-end: var(--space-xs);
    }
  }
{%- endcss -%}
